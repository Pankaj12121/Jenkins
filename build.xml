<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. -->
<project basedir="." default="testng-execution" name="JenkinsPro">
	
	<property name ="basedir" value="." />
	<property name ="libs.dir" value="${basedir}/libs" />
	<property name ="src.dir" value="${basedir}/src" />
	<property name ="bin.dir" value="${basedir}/bin" />
	<property name ="report-dir" value="${basedir}/Test-Report" />
	<property name ="testng-report-dir" value="${basedir}/TestNGreport" />
	
	<taskdef resource="testngtasks" classpath="C:\\Users\\pankajs\\.m2\\repository\\testng-6.11.jar"/>
	
	<!-- ====== Set the classpath ====  -->
		<path id="classpath">
			<pathelement location="${bin.dir}" />
			<fileset dir="${libs.dir}">	
				<include name="*.jar" />
			</fileset>	
		</path>
		
	   <path id="libs">
			<fileset dir="${libs.dir}">
				<include name="**/*.jar" />
			</fileset>
			<fileset dir="C:\\Users\\pankajs\\.m2\\repository">
				<include name="**/*.jar" />
			</fileset>
			<pathelement path="${basedir}/${bin.dir}"/>
		</path>
	
		<!-- Delete directories  -->
		<target name="delete-dir">
			<delete dir="${bin.dir}" />
			<delete dir="${report-dir}" />
		</target>
		
		<!-- Creating directories -->
		<target name="create" depends="delete-dir">
			<mkdir dir="${bin.dir}" />
			<mkdir dir="${report-dir}" />
		</target>
		
		<!-- Compile the java code from ${src} into ${bin} -->
		<target name="compile" depends="create">
			<javac srcdir="${src.dir}" classpathref="libs" includeAntRuntime="No" destdir="${bin.dir}" />
			<echo>  Compiled Directory Classes  </echo>
		</target>
		
		<!-- Runs the file and generates Reportng report for TestNG-->
		<!--taskdef name="testng" classname="org.testng.TestNGAntTask" classpathref="classpath" /!-->
	
	<target name="testng-execution" description="Running tests" depends="compile">
			<echo>Running Tests...</echo>
			<java  dir="${basedir}/${src.dir}" fork="false" failonerror="yes" classname="com.demo.testNG.AssertVsVerify.TestAnt.java" classpathref="libs">
				  <!--jvmarg value="-Xmx1024M"/>-->
				<!-- <arg value="${currentExecutionResults}"/> -->
				<!-- <compilerarg line="-J-Xms512M -J-Xmx1024M" />-->
			</java>
		</target>
		
	
	
	<target name="CompleteSmokeSuite">
			<antcall target="testng-execution"/>
		</target>

</project>
